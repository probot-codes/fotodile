// verification.ts

export interface InstagramProfileInfo {
  username: string;
  full_name: string;
  biography: string;
  profile_pic_url: string;
  is_private: boolean;
  num_posts: number;
  num_followers: number;
  num_follows: number;
  external_url: string;
}
